Each participant will be in charge of their own node. Each node could install instances of fediverse apps. We could access our node through multiple **clients**.  I guess we'll let the community write the clients they want.

So we need to find a way to easily install them on own server. We also need to figure out how to manage our data. Do we keep a source of truth per instance? 